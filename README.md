# LM23-with-Arduino-thermometer
The LM35 sensor measures temperature and gives an analog voltage output proportional to the temperature (10 mV per °C). This signal is sent to the Arduino Uno, which reads it through an analog input pin, converts it into temperature (in °C), and processes the valu
